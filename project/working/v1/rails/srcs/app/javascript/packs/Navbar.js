import $ from "jquery"
import _ from "underscore"
import Backbone from "backbone"

const Navbar = {};

if ($('html')) {
    $(() => {
        /* Model */
        /* View */
        /* instanciate view */
    })
}

export default Navbar;